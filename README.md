# 리뷰팝 (ReviewPop)

> 체험단 서비스 플랫폼

## 📋 목차

- [소개](#-소개)
- [기술 스택](#-기술-스택)
- [시작하기](#-시작하기)
- [폴더 구조](#-폴더-구조)
- [협업 가이드](#-협업-가이드)

### 주요 기능

- (추가 기능은 개발 진행에 따라 업데이트)

## 기술 스택

### Frontend

- **Framework**: Next.js 16.0.1 (App Router)
- **UI Library**: React 19.2.0
- **Language**: TypeScript 5.x
- **Styling**: SCSS (Sass)
- **Data Fetching**: axios, Tanstack Query
- **State Management**: Zustand

### Development Tools

- **Package Manager**: yarn
- **Code Quality**: ESLint, Prettier
- **Version Control**: Git (Git Flow)

### DevOps

- **CI/CD**: GitHub Actions
- **Deployment**: (미정)

## 시작하기

### Prerequisites

다음 소프트웨어가 설치되어 있어야 합니다:

- **Node.js**: v20.x 이상
- **yarn**: v1.22.x 이상

### Installation

1. 저장소를 클론합니다:

```bash
git clone https://github.com/datepop/reviewpop-web.git
cd reviewpop-web
```

2. 의존성을 설치합니다:

```bash
yarn install
```

3. 환경 변수를 설정합니다 (필요한 경우):

```bash
# .env.local 파일을 생성하고 필요한 환경 변수를 설정합니다
# (환경 변수가 필요한 경우 팀에 문의하세요)
```

### Development

```bash
yarn dev # 개발 서버를 실행
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 결과를 확인할 수 있습니다.

### Production

프로덕션 빌드를 생성하고 서버를 실행합니다:

```bash
yarn build # 빌드 생성
yarn start # 서버 실행
```

## 📁 폴더 구조

```
reviewpop-web/
├── public/              # 정적 파일 (이미지, 아이콘 등)
├── src/
│   ├── app/            # Next.js App Router 페이지 및 라우팅
│   │   ├── layout.tsx  # 루트 레이아웃
│   │   └── page.tsx    # 홈 페이지
│   ├── components/     # 재사용 가능한 React 컴포넌트
│   ├── assets/         # 정적 자산 (폰트, 이미지 등)
│   ├── styles/         # 전역 스타일 및 스타일 유틸리티
│   │   ├── globals.scss    # 전역 스타일
│   │   ├── fonts.ts        # 폰트 설정
│   │   ├── colors.ts       # TypeScript 색상 상수
│   │   ├── _colors.scss    # 색상 변수
│   │   └── _reset.scss     # CSS 리셋
│   ├── components/      # 재사용 가능한 컴포넌트
│   ├── hooks/           # 커스텀 훅
│   ├── services/        # API 서비스 로직
│   ├── stores/          # 상태 관리 스토어
│   ├── utils/           # 유틸리티 함수
│   ├── types/           # TypeScript 타입 정의
│   └── constants/       # 상수 정의
├── .eslintrc.js        # ESLint 설정
├── .prettierrc         # Prettier 설정
├── next.config.ts      # Next.js 설정
├── tsconfig.json       # TypeScript 설정
└── package.json        # 프로젝트 의존성
```

## 🤝 협업 가이드

### Git 브랜치 전략 (Git Flow)

이 프로젝트는 Git Flow 전략을 따릅니다:

#### 주요 브랜치

- **`main`**: 프로덕션 배포 브랜치
  - 항상 배포 가능한 상태를 유지합니다
  - 직접 커밋하지 않습니다
  - `develop` 또는 `hotfix` 브랜치에서만 병합됩니다

- **`develop`**: 개발 통합 브랜치
  - 다음 릴리스를 위한 개발 작업이 통합되는 브랜치
  - 기능 개발이 완료되면 이 브랜치로 병합됩니다

#### 지원 브랜치

- **`feature/*`**: 새로운 기능 개발
  - 명명 규칙: `feature/기능명` (예: `feature/user-authentication`)
  - `develop` 브랜치에서 분기합니다
  - 개발 완료 후 `develop` 브랜치로 병합합니다

- **`hotfix/*`**: 프로덕션 긴급 수정
  - 명명 규칙: `hotfix/버그명` (예: `hotfix/login-error`)
  - `main` 브랜치에서 분기합니다
  - 수정 완료 후 `main`과 `develop` 브랜치 모두에 병합합니다

#### 브랜치 작업 흐름

1. **기능 개발 시작**:

```bash
git checkout develop
git pull origin develop
git checkout -b feature/new-feature
```

2. **개발 작업 및 커밋**:

```bash
# 작업 후
git add .
git commit -m "feat: 새로운 기능 추가"
```

3. **원격 저장소에 푸시**:

```bash
git push origin feature/new-feature
```

4. **Pull Request 생성**:
   - GitHub에서 `feature/new-feature` → `develop`으로 PR 생성
   - 코드 리뷰 후 병합

### 커밋 컨벤션 (Conventional Commits)

Conventional Commits 규칙을 따릅니다:

#### 커밋 메시지 형식

```
<type>: <subject>

- <body ...>

<footer>
```

#### 타입 (Type)

- **feat**: 새로운 기능 추가
- **fix**: 버그 수정
- **docs**: 문서 수정 (README, 주석 등)
- **style**: 코드 포맷팅, 세미콜론 누락 등 (코드 동작에 영향 없음)
- **refactor**: 코드 리팩토링 (기능 변경 없음)
- **test**: 테스트 코드 추가 또는 수정
- **chore**: 빌드 설정, 패키지 매니저 설정 등
- **perf**: 성능 개선
- **ci**: CI/CD 설정 변경

#### 예시

```bash
# 기본 형식
feat: 사용자 로그인 기능 추가

# 본문 포함
fix: 리뷰 작성 시 이미지 업로드 오류 수정

- 이미지 파일 크기 제한을 5MB로 증가
- 업로드 실패 시 사용자에게 적절한 에러 메시지를 표시하도록 수정
```

### 코드 컨벤션

#### ESLint 설정

프로젝트는 다음 ESLint 규칙을 따릅니다:

- Next.js Core Web Vitals 규칙
- TypeScript 권장 규칙
- Prettier와 통합

설정 파일: `eslint.config.mjs`

#### Prettier 설정

주요 Prettier 규칙:

```json
{
  "printWidth": 100,
  "tabWidth": 2,
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "arrowParens": "always"
}
```

설정 파일: `.prettierrc`

#### 코딩 스타일 가이드

- **들여쓰기**: 2 spaces
- **따옴표**: 작은따옴표 (') 사용 (JSX 제외)
- **세미콜론**: 항상 사용
- **화살표 함수**: 컴포넌트 및 함수 정의 시 사용
- **명명 규칙**:
  - 변수/함수: camelCase
  - 컴포넌트: PascalCase
  - 상수: UPPER_SNAKE_CASE
  - 디렉토리: kebab-case

자세한 내용은 프로젝트의 ESLint 및 Prettier 설정을 참조하세요.

## 📚 API 문서

> 🚧 API 문서는 개발이 진행에 따라 추가

API 문서 링크: (TBD)
